{
  "parameters": [
    {
      "name": "date",
      "in": "query",
      "schema": { "type": "string", "example": "YYYY-MM-DD" },
      "description": "Filter the statistics by specific date"
    },
    {
      "name": "timezone",
      "in": "query",
      "schema": { "type": "string", "example": "Your account's timezone" },
      "description": "Filter the statiscs with the timezone applied if not it returns the UTC time"
    },
    {
      "name": "id",
      "in": "path",
      "required": true,
      "schema": { "type": "string", "example": "Type a Profile ID" },
      "description": "Can be found in the profile's information tab \"More\""
    }
  ]
}
