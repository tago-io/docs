{
  "title": "Body",
  "body": {
    "content": {
      "application/json": {
        "schema": {
          "type": "object",
          "required": ["email", "password"],
          "properties": {
            "email": { "type": "string", "example": "example@mail.com" },
            "password": { "type": "string", "example": "yourpassword" }
          }
        },
        "examples": { "Login to Account": { "value": { "email": "example@mail.com", "password": "yourpassword" } } }
      }
    }
  }
}
